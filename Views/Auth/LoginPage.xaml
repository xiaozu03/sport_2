<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:oculus_sport.ViewModels.Auth"
             x:Class="oculus_sport.Views.Auth.LoginPage"
             Title="Login"
             Shell.NavBarIsVisible="False"
             BackgroundColor="{StaticResource White}">

    <ScrollView>
        <VerticalStackLayout Spacing="25" Padding="30" VerticalOptions="Center">

            <Image Source="uts_logo.png" 
                   HeightRequest="180" 
                   HorizontalOptions="Center" 
                   Aspect="AspectFit" 
                   Margin="0,20,0,10"/>

            <Label Text="OCULUS SPORT" 
                   FontSize="20" 
                   TextColor="{StaticResource Gray600}" 
                   HorizontalOptions="Center" 
                   CharacterSpacing="4"/>

            <Label Text="Welcome back" 
                   FontSize="28" 
                   TextColor="{StaticResource Black}" 
                   FontAttributes="Bold"
                   HorizontalOptions="Center"
                   Margin="0,10,0,10"/>

            <VerticalStackLayout Spacing="15">
                <Border Stroke="{StaticResource Gray300}" StrokeThickness="1" StrokeShape="RoundRectangle 10" Padding="15,0">
                    <Entry Placeholder="Username or Email" 
                           Text="{Binding Email}"
                           TextColor="{StaticResource Black}"
                           PlaceholderColor="{StaticResource Gray400}"
                           HeightRequest="50"/>
                </Border>

                <Border Stroke="{StaticResource Gray300}" StrokeThickness="1" StrokeShape="RoundRectangle 10" Padding="15,0">
                    <Entry Placeholder="Password" 
                           IsPassword="True"
                           Text="{Binding Password}"
                           TextColor="{StaticResource Black}"
                           PlaceholderColor="{StaticResource Gray400}"
                           HeightRequest="50"/>
                </Border>
            </VerticalStackLayout>

            <Button Text="Log in" 
                    Command="{Binding LoginCommand}"
                    BackgroundColor="{StaticResource Primary}"
                    TextColor="White"
                    CornerRadius="25"
                    HeightRequest="50"
                    FontAttributes="Bold"
                    Margin="0,10,0,0"/>

            <ActivityIndicator IsRunning="{Binding IsBusy}" 
                               IsVisible="{Binding IsBusy}"
                               Color="{StaticResource Primary}"
                               HorizontalOptions="Center"/>

            <HorizontalStackLayout HorizontalOptions="Center" Spacing="5">
                <Label Text="Don't have an account? " TextColor="{StaticResource Gray500}"/>
                <Label Text="Sign up" 
                       TextColor="{StaticResource Primary}" 
                       FontAttributes="Bold"
                       TextDecorations="Underline">
                    <Label.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding GoToSignUpCommand}"/>
                    </Label.GestureRecognizers>
                </Label>
            </HorizontalStackLayout>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>